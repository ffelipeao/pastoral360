Leia integralmente:

- AGENTS.md
- {SPEC_PATH}
- reference.md na mesma pasta da spec, quando existir

Analise o projeto antes de realizar alterações. Espelhe padrões existentes em `v2/` (especialmente páginas e classes de referência citadas na spec).

Implemente exclusivamente:

{STEP_TITLE}

Conteúdo da etapa:

{STEP_CONTENT}

Regras obrigatórias:

1. Não implemente requisitos pertencentes às próximas etapas.
2. Preserve o comportamento existente da aplicação.
3. Classes PHP em `v2/classes/`; páginas em `v2/`; SQL apenas nas classes quando a spec exigir.
4. Não altere o arquivo `.env` nem inclua credenciais ou segredos.
5. Não execute push, merge, rebase ou reset.
6. Reutilize CSS, includes e componentes visuais existentes — não invente design novo.
7. Valide sintaxe PHP dos arquivos alterados (`php -l`).
8. Não declare a etapa como concluída se houver erro de sintaxe ou critérios de aceite pendentes.
9. Não prossiga para nenhuma outra etapa.
10. Marque checklists da spec/reference quando aplicável.

Ao finalizar, apresente:

STATUS: CONCLUÍDA ou FALHOU

RESUMO:
- descrição objetiva da implementação;

ARQUIVOS:
- arquivos criados;
- arquivos modificados;

VALIDAÇÃO:
- critérios de aceite atendidos;
- comandos `php -l` executados e resultados;

PENDÊNCIAS:
- itens não implementados;
- dependências;
- riscos encontrados.

## Mensagem para commit

Inclua ao final (mesmo sem executar git commit) um bloco neste formato exato, em português do Brasil:

```
<tipo>: <resumo objetivo em uma linha>.

<opcional: 1–3 bullets curtos com o essencial do que mudou>
```

Regras: seguir `.agents/rules/mensagem-commit.md`; usar feat/fix/refactor/chore/docs/test/style/perf; focar no porquê e no impacto; manter esta seção como o último bloco da resposta. Não execute o commit.
